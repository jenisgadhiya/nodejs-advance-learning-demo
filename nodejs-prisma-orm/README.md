# Setup database and add database uri in env file

# Run prisma migration

```bash
npx prisma migrate dev --name init
```

# Install Dependencies

```bash
npm install
```

## Run Project

```bash
npm run dev
```
